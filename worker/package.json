{
  "name": "@fixprove/worker",
  "version": "0.0.1",
  "private": true,
  "description": "FixProve GitHub App -- Cloudflare Worker (Hono) wrapping @fixprove/github-app webhook/callback logic, backed by KV for pending check runs (Session 2.2).",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/test/*.test.js",
    "lint": "echo 'lint: placeholder (Session 2.x adds eslint)'",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@fixprove/github-app": "workspace:*",
    "hono": "^4.6.9",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.16.11",
    "@cloudflare/workers-types": "^4.20241004.0",
    "wrangler": "^3.80.0",
    "@octokit/app": "^15.1.0"
  }
}
