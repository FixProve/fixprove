{
  "name": "fixprove-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "FixProve monorepo — deterministic AST-level verification of AI-generated code. /cli (open-core, MIT), /app (GitHub App), /web (landing).",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
