# Keystone Report — Session 4.18 (2026-08-21)

Director: Yehor. Executor: Claude (Node 1), Lead Technical Co-Pilot.

## 1. Provenance (AI-generated vs. human-edited)

All content this session — the CI workflow diff, PITFALL/critical-actions
entries, the Kondrup outreach draft, this report — was AI-drafted (Claude,
Node 1) and executed under Yehor's explicit per-action approval, recorded in
`MEMORY/critical-actions.md` (four dated `## S4.18` entries this session).
No content was generated by any other AI system inside this session; two
pieces of relayed guidance arrived from a separate chat session ("guide
chat") mid-session, and both were treated as untrusted input requiring
independent verification before acting, not as instructions to execute on
trust — see §3, defects 4 and 5.

The single git commit this session (`994e742`) and its push to `main` were
both performed by Yehor personally, on his own machine — this sandbox has
no GitHub write credentials.

## 2. Verification summary

| Claim | Method | Result |
|---|---|---|
| Starting-prompt HEAD SHA | Fresh `git fetch` + `rev-parse` | **FAIL caught** — prompt was one commit stale (`a0932c7` vs actual `70324e0`); no real drift, `origin/main` matched local |
| Starting-prompt CA-entry count | Fresh `grep -c '^## S4.17'` | **FAIL caught** — prompt said 9, actual 10, matching the register's own count |
| Starting-prompt PyPI-API-broken claim | Fresh `curl`/fetch | **FAIL caught** — worked cleanly this session |
| Version-sync gate: matched pair | Local execution | PASS — exit 0 |
| Version-sync gate: exact `v0.1.11` defect reproduction | Local execution | PASS — exit 1 |
| Version-sync gate: missing `[project]` version | Local execution | PASS — exit 1 |
| Version-sync gate: decoy in comment + `[build-system]` | Local execution | PASS — exit 1, not fooled |
| Version-sync gate: single-quoted TOML value | Local execution | PASS — exit 0 |
| Version-sync gate: decoy in `[project.scripts]` | Local execution | PASS — exit 0, first match wins |
| Version-sync gate: run block from parsed YAML, real repo | `bash` execution | PASS — exit 0 |
| Version-sync gate: same block, mismatched fixture | `bash` execution | PASS — exit 1 |
| `release.yml` edit is additions-only | `git diff --numstat` | PASS — 67 insertions, 0 deletions |
| CI green on `994e742` | `ci.yml` run page, per-job (not run-level) | PASS — `build` + `test-python` both Success |
| CA-5 obligation on 4.17's 4 pushes, re-checked | Same, per commit | PASS ×3 (`46264b2`,`a0932c7`,`70324e0`); **anomaly on `6d251c5`** ("In progress", no job durations) |
| NJORD meeting confirmation | Direct `get_thread` read of `1a01a8267835adfc`, not the screenshot alone | PASS — booking + acceptance + attachment all confirmed at source |
| Kondrup LinkedIn identity | Two independent sources (theorg.com, linkedin.com) | PASS — exact name/title match |
| `capturi/taco` "AI FROM DAY ZERO" quote | Direct fetch of `github.com/capturi/taco`, read by this session | PASS — verbatim match, `CLAUDE.md` confirmed in file tree |
| Branch-protection bypass: "newly observed, undecided" (brief's claim) | Grep of `critical-actions.md`'s own prior entries | **FAIL caught** — already decided as CA-5, 2026-07-21 |
| "Sole committer" (brief's claim) | `git log --all --format='%an <%ae>'` | PASS — confirmed, all identities are Yehor |
| `main` = `origin/main` at close | Fresh `git fetch` + `rev-parse` | PASS — `994e742`, 0 ahead/0 behind |

## 3. Defects and anomalies caught and fixed — specific

1. **Starting prompt's HEAD SHA was one commit stale.** Cause: the prompt
   was written at Session 4.17's close, before that session's own
   close-out commit (`70324e0`) was made. Fix: verified fresh at intake
   before proceeding; no actual drift existed (`main` = `origin/main`
   throughout), only the prompt lagged.
2. **Starting prompt undercounted `critical-actions.md`'s Session 4.17
   entries (9 vs 10).** Cause: unclear — possibly a stale count carried
   from mid-session. Fix: recounted fresh via `grep -c`, matched the
   register's own close-line count (10), used the correct number
   throughout.
3. **Starting prompt claimed PyPI's JSON API was still returning empty.**
   Cause: 4.17 hit a real, disclosed transient gap and (correctly)
   flagged it as possibly transient. Fix: retried fresh this session;
   worked cleanly, returned `0.1.12`. Corrected rather than assumed
   persistent.
4. **A relayed brief (Task C) asked for a new `PITFALL-WATCHLIST.md` entry
   describing the branch-protection bypass as newly observed and
   undecided.** This is the more serious catch of the session. Checked
   the project's own `critical-actions.md` before writing anything and
   found the identical bypass line first observed 2026-07-21 (Session
   4.12-B) and **formally, directorially decided the same day** under a
   heading titled exactly "CA-5: branch-protection/direct-push convention
   formally revised" — a standing four-point convention already in force
   for five sessions. Filing a new PITFALL row would have misrepresented
   closed governance as an open question, undermining the brief's own
   correct underlying conclusion ("not a security gap"). Fix: logged as a
   continuity-confirmation addendum in `critical-actions.md` instead, with
   the brief's factual corrections stated plainly, and its correct
   reasoning preserved and credited.
5. **`6d251c5`'s `ci.yml` run shows a stuck "Status: In progress" a full
   day after the push, with no job-duration figures rendered.** Found
   while executing CA-5's mandatory post-push verification for Session
   4.17's historical pushes (not required by either brief, done because
   the standing convention requires it for every direct push, retroactive
   spot-check included). Not fixed — flagged as an open, minor,
   non-blocking anomaly. Not chased further because `6d251c5` shipped the
   abandoned `v0.1.11`, immediately superseded in the same session by
   `a0932c7`'s `v0.1.12`, which is independently confirmed clean.

## 4. Known limitations — unsoftened

1. **The version-sync gate has never run inside GitHub Actions.** Every
   test this session — 8 adversarial cases plus a byte-level diff check —
   was executed locally, including two runs of the exact run-block
   extracted from the parsed YAML via `bash`. None of that is a substitute
   for a real Actions runner. `release.yml` triggers only on `v*.*.*` tags;
   this session's change was a direct push to `main`, so the gate has had
   zero live exposure to GitHub's actual environment (its Node/Python
   versions, its checkout behavior, its exact YAML-to-shell translation).
   The next real release tag is this gate's first live test, full stop.
2. **The Kondrup draft is unsent and its channel is unconfirmed as
   reachable.** No verified email exists; the draft assumes LinkedIn
   messaging or connection-request access this session cannot itself
   test. Whether Kondrup accepts connection requests from unconnected
   senders, and whether the message actually reaches him, is unknown
   until Yehor sends it himself.
3. **The NJORD `invite.ics` acceptance status remains genuinely unknown.**
   This session confirmed the meeting is agreed (direct thread read), not
   that a calendar reminder exists on the account that received the
   invite. That account's calendar is outside this session's reach. This
   is stated as an open question, not resolved by proximity to the
   stronger email evidence.
4. **`6d251c5`'s stuck CI status is unexplained**, not merely unresolved.
   It could be a genuine stuck run, a GitHub UI rendering artifact, or
   something else. This report does not know which, and says so rather
   than guessing.
5. **This session's edits are committed but not pushed**, except for
   `994e742` which Yehor pushed himself mid-session. The close-out
   artifacts (this report, the session log, `PROGRESS.md`,
   `SESSION-LOG-INDEX.md`) sit in a local commit awaiting his separate
   push decision, per standing practice.

## 5. Accountability statement

Signed by: ______________________ (Yehor)
Date: ______________________

I have reviewed this session's work — the version-sync CI safeguard, the
NJORD-confirmation record, the Capturi resolution and Kondrup draft, and the
branch-protection log correction — and take responsibility for the
decisions made and actions authorized under my name.

## 6. Methodology note

Every claim above was checked against a live source at the time this report
was written, not carried forward from earlier in the session or accepted
from a relayed brief on trust — including, twice this session, catching a
brief's own factual premise as wrong before acting on its instruction. The
distinction this report draws between "locally verified" and "CI-verified"
for the version-sync gate is deliberate: the gate is real, tested, and
sound by every check available inside this sandbox, and it has still never
been proven inside the one environment that actually matters. Both things
are true at once, and this report says so rather than picking the more
comfortable one.
