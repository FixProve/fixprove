node_modules/
dist/
.next/
out/
.turbo/
*.log
.env
.env.local
.DS_Store

# Secrets -- never commit these. GitHub App private keys download as .pem;
# this line exists specifically because Session 3.2's live deployment
# runbook execution found one had landed inside this repo folder by
# accident (browser default download location, not intentional).
*.pem
*.key
.dev.vars

# Python build artifacts (engine/python/)
__pycache__/
*.py[cod]
.pytest_cache/
.hypothesis/
build/
*.egg-info/
.fixprove_cache/

# Rust build artifacts (placeholders/crates-fixprove/)
target/

# Business/financial documents -- not source, shouldn't be in version control
invoices/

# Leftover write-verify-move scratch artifacts
*.check

# Financial/legal business documents (Session 4.12-B) -- never commit
Bookkeeping*/

# Next.js auto-generated type declaration -- regenerated by `next build`/
# `next dev` every time; committing it just creates unnecessary diff noise
# and risks drifting from the installed Next.js version (Session 4.12-B)
web/next-env.d.ts

# Internal governance memory -- confirmed never tracked at HEAD (`git
# ls-files -- MEMORY/` returned empty, 2026-07-21). Session state/decision
# logs stay local; the exposure check found these files necessarily name
# sensitive due-diligence findings (e.g. a redacted comparison-mark's
# identity) when explaining what was redacted and why -- that's correct
# for an internal audit trail but wrong for public history. NOTE: this
# does NOT extend to session-logs/ or NEXT-SESSION-*-STARTING-PROMPT.md --
# both have substantial pre-existing tracked history (9 and 7 files
# respectively, confirmed via `git ls-files` the same session) and a
# blanket gitignore there would split old-public/new-private inconsistently.
# That is a separate, deliberate decision, not made this session -- see
# MEMORY/critical-actions.md, 2026-07-21 entry.
MEMORY/

# Live internal progress dashboard -- Q1 decision, 2026-07-21 (see
# MEMORY/critical-actions.md): this file catalogues current weaknesses
# (MRR, paying-org count, every milestone's PAUSED/MISSING status) in one
# place and was never tracked at HEAD (git ls-files confirmed empty), so
# this is a clean gitignore with no history to split. A separate curated
# public-status document may be published later if a public progress
# signal is wanted; this raw dashboard stays internal-only.
PROGRESS.md

# Session-close dry-run catch, 2026-09-03 (Session 4.25): this directory
# holds only empty (0-byte) orphaned git lock-file markers moved aside by
# the standing "mv, never rm" lock-handling rule -- operational noise, not
# project history. `git ls-files -- .git-stale-locks/` confirmed empty.
.git-stale-locks/

# Session-close dry-run catch, 2026-09-03 (Session 4.25): compiled JS
# output of web/functions/*.ts (confirmed via matching source files, e.g.
# web/functions/api/_validate.ts -> web/functions-dist/api/_validate.js).
# A build artifact, same category as dist/ above; never tracked at HEAD.
web/functions-dist/
