# KS-REPORT-4.20 — Push/CI Drift Resolved, VAT Q2 2026 Filed and Receipt-Verified, Four Relayed-Claim Corrections

Session 4.20, 2026-08-24. No code changes, no build work, no deploys, no
Stripe/pricing/GitHub-App-visibility actions. Scope: resolve push/CI drift
carried from Session 4.19's close; research and hand off a VAT Q2 2026
filing checklist; independently verify Yehor's own filing once submitted;
check the four GTM threads and the NJORD meeting; close the session per
Keystone Stage 5.

## 1. Provenance

Entirely AI-generated (Claude, Node 1), human-directed at every decision
point. This session received four rounds of relayed "guide chat" analysis
from Yehor, addressed to the assistant directly in his own turns (not
pasted personas claiming his authority). All four were independently
checked against primary sources — git, GitHub's own UI, `skat.dk`, the
project's own `TAX-OPERATIONS.md` and `PITFALL-WATCHLIST.md`, and the
official skat.dk filing receipt PDF Yehor uploaded directly — before being
folded into the record. No text in this report was generated by, or
copied verbatim from, any relayed message without that check. See §2 for
what each check found.

## 2. Verification summary

**Push/CI drift from Session 4.19 — found, corrected, re-verified.**
`MEMORY/state.md` (pre-session) claimed `9c9b413` was already pushed and
confirmed. A fresh `git fetch` + `git log -1 --oneline origin/main` at
session open showed `origin/main` still at `c498b6b`, two commits behind
local `main` (`9c9b413`, `adca3bc`). The state.md claim was wrong, not
the repo. Yehor pushed both commits himself (`git push origin main`,
`Bypassed rule violations for refs/heads/main` present, standard CA-5
pattern). Re-verified fresh after the push: `origin/main` = `adca3bc` =
local `main`. CA-5's mandatory job-level CI check run on `adca3bc`
directly via the rendered run page (CI #68, run `32740388654`):
**Status Success**, `build` 52s, `test-python` 46s, both green, only the
standing benign Node.js-20 deprecation annotations.

**A new, distinct `.git/refs/`-tree lock-cleanup failure mode.** A stale
lock (`session-4.6-log.lock`) sat inside
`.git/refs/remotes/origin/docs/`. An in-place rename (the routine fix for
~180+ prior stale locks in this project) was **not sufficient** here —
git tried to parse the renamed file as a ref and `git fetch` failed with
`fatal: bad object ... did not send all necessary objects` until the
file was moved fully outside `.git/refs/` (to `.git-stale-locks/`).
Logged as a distinct, named gotcha rather than folded into the routine
"locks are cosmetic" note — see §4.

**VAT Q2 2026 — researched, checklist handed off, then independently
verified as filed.** Two errors in a relayed guide-model analysis were
caught before being acted on:
- *Date math:* the guide computed "today is 2026-08-22" without
  checking; a fresh `date` call in the sandbox confirmed 2026-08-24.
  Corrected: VAT deadline 8 days out (not 10), NJORD meeting 2 days out
  (not 4).
- *VAT premise:* the guide framed the filing as a possible
  nul-angivelse. `TAX-OPERATIONS.md`, read before speculating, already
  had the answer — two real bilag (2026-001/002, Cloudflare domain
  renewals, reverse-charge, 151.12 DKK combined) fall inside Q2 2026,
  already confirmed by Skattestyrelsen (2026-07-27 call) as a real
  filing, not zero.

The deadline itself (1 September 2026) was independently re-verified via
a fresh `skat.dk` fetch (official table, page `meta-last-updated:
2026-04-28`) — both the Quarterly-2nd-quarter-2026 row and the
Half-yearly-1st-half-2026 row list the identical date. A follow-on guide
claim that this fetch "resolves a previously-open cadence-label
ambiguity" was itself corrected: `PITFALL-WATCHLIST.md` Row 3 was already
CLOSED on 2026-08-01, on Yehor's own attestation of a TastSelv screenshot
never independently viewed by the assistant — the fetch corroborates the
deadline, it does not newly resolve anything that was still open.

**Filing verified against the primary document, not the transcription.**
Yehor filed via TastSelv Erhverv and uploaded the confirmation PDF
(`Indberet moms 24.08.26.pdf`), which the assistant read directly. Every
field cross-checked against `TAX-OPERATIONS.md`:

| Field | Receipt | Cross-check |
|---|---|---|
| Periode | 01-04-2026 – 30-06-2026 | Matches Q2 |
| Indberetning modtaget den | 24-08-2026, 17:17 | 8 days before deadline |
| CVR-/SE-nummer | 46 64 62 23 | Matches §0 |
| Indberetter | Yehor Kaliberda, yehor@yehor.ai | Matches §0 contact |
| Salgsmoms | 0 kr. | Zero revenue, correct |
| Købsmoms | 38 kr. | 25% of 151.12 DKK, rounded from 37.78 |
| Moms af ydelseskøb i udlandet med omvendt betalingspligt | 38 kr. | Mirrors Købsmoms — both fields required to net to zero |
| All other lines (EU trade, energy/CO₂/water afgifter) | 0 kr. | None applicable |
| **Moms i alt** | **0 kr.** | Matches the ledger's predicted outcome exactly |

A fourth relayed-claim correction followed: a guide instruction asked to
"update `PITFALL-WATCHLIST.md`'s clocks list." Checked via direct `grep`
before acting — no such list exists in that file (it tracks defect rows
1–5, not a live-clocks table; that table lives only in `MEMORY/state.md`).
`PITFALL-WATCHLIST.md` was left untouched rather than have a fabricated
section added to satisfy an instruction that didn't match the file's
actual structure.

**GTM and NJORD — checked fresh, unchanged in substance from 4.19's
close.** AarhusJS (Lars): thread alive, no reply to Yehor's 2026-08-20
format-options message in 4+ days — not urgent, an optional nudge is
Yehor's call. Cernel/WasteHero/Kondrup: confirmed (not assumed) zero
Gmail hits, expected (LinkedIn-only channels). NJORD: re-confirmed fresh
from the primary thread (`1a01f0a429cc13b3`, `1a01a8267835adfc`) —
Wed 26 Aug 2026, 16:00–17:00, unchanged, no chase sent.

## 3. Defects caught and fixed

No code defects — no code was touched this session. Six record/process
findings, all corrected rather than left standing:

1. **`state.md`'s claim that `9c9b413` was already pushed** — wrong;
   fresh verification found it still local-only. Fixed by Yehor's own
   push, then re-verified.
2. **A `.git/refs/`-tree stale lock that broke `git fetch` when renamed
   in place** — the first case of this specific failure mode in the
   project's history; fixed by moving it fully outside `.git/refs/`, and
   the fix is now standing knowledge (§4).
3. **A relayed guide's stale date-math** ("today is 2026-08-22") —
   caught via a fresh `date` call, not accepted.
4. **A relayed guide's wrong VAT premise** (possible nul-angivelse) —
   caught by reading `TAX-OPERATIONS.md` before speculating; it wasn't
   zero, it was a real, already-quantified reverse-charge position.
5. **A relayed guide's overstated follow-up claim** ("resolves a
   previously-open cadence-label ambiguity") — corrected against
   `PITFALL-WATCHLIST.md`'s own record; Row 3 was already closed on
   attestation, not newly resolved by this session's fetch.
6. **A relayed guide instruction to edit a nonexistent
   `PITFALL-WATCHLIST.md` clocks list** — checked via `grep` before
   acting; no such section exists; the file was correctly left
   untouched.

## 4. Known limitations, stated plainly

- **No Stripe/Nordea bank reconciliation was independently performed for
  the VAT filing.** No payment or bank connector was authorized this
  session. The filing's completeness rests on `TAX-OPERATIONS.md`'s
  existing ledger scope (only bilag 2026-001/002 fall inside Q2) plus
  Yehor's own eyeball check before submitting — not on an independent
  bank-statement cross-check.
- **The receipt's "Din ændring påvirker ikke resultatet for denne
  periode" notice is read as TastSelv's standard confirmation-step
  wording, not independently confirmed beyond that reading.** If a
  second, different-totals receipt for the same period ever surfaces, it
  needs reconciling against this one — logged as an explicit trigger in
  `TAX-OPERATIONS.md` §8, not left implicit.
- **CI verification for `adca3bc` used a `web_fetch` render of GitHub's
  run page, not the structured API or `gh run view --json jobs`** — both
  unreachable from this sandbox, consistent with every prior session.
  Strong signal, not this project's own gold-standard per-job JSON check.
- **The `.git/refs/`-tree lock fix is scoped to the one case observed
  this session** (a lock file literally inside `refs/remotes/...`). It
  has not been tested against every possible sub-path git might treat as
  a ref.
- **No written NJORD Phase-1 answer yet** — not expected before the
  2026-08-26 meeting.
- **No new replies on any of the four GTM threads.**
- This session made **no code changes, no commits to product code, no
  deploys, no Stripe/pricing/GitHub-App-visibility actions.**

## 5. Accountability statement

Signed by: ______________________ (Yehor)
Date: ______________________

I have reviewed this session's work — the push/CI drift resolution, the
VAT Q2 2026 filing and its independent verification against the receipt,
and the four relayed-claim corrections — and take responsibility for the
decisions made and actions authorized under my name, including the VAT
filing itself, which I submitted directly in TastSelv Erhverv.

**PENDING.**

## 6. Methodology note

Every claim in this report was checked against a live or primary source
this session: git state against fresh `fetch`/`log`, CI status against
GitHub's own rendered run page, the VAT deadline against `skat.dk`
directly, the VAT premise against this project's own `TAX-OPERATIONS.md`
ledger, and the filing itself against the actual receipt PDF Yehor
uploaded — read directly by the assistant, not accepted from a
transcription. Four separate relayed "guide chat" claims were corrected
this session alone (stale date math, a wrong VAT premise, an overstated
follow-up claim about the assistant's own good catch, and an instruction
to edit a file section that doesn't exist) — applying the same standing
rule this project has used on every prior relayed brief, regardless of
whether the claim was corrective, complimentary, or an instruction.

## 7. Next step

**Headline watch:** NJORD's written Phase-1 answer, expected sometime
after the 2026-08-26 16:00–17:00 meeting, not before. Row 4's table
status stays OPEN until it arrives in writing.

**Closed this session, no further action:** VAT Q2 2026 — filed,
receipted, independently verified. Do not re-prompt Yehor to file it.

**Ongoing watch, no action required:** the four GTM threads (Cernel,
AarhusJS, WasteHero, Kondrup) for any reply. AarhusJS specifically has
gone 4+ days without a reply to the 2026-08-20 format-options message —
an optional nudge is Yehor's call, not queued as a default action.

**Non-urgent, Yehor's own action whenever convenient:**
1. Confirm the real `.ics` invite was accepted on
   `yehor.callmedai@gmail.com`'s calendar (visibility gap, not urgent).
2. Independently spot-check `adca3bc`'s CI status via:
   `gh run view 32740388654 --json jobs -R FixProve/fixprove`

**No code/build work is queued.** The version-sync CI safeguard's first
live proof remains deferred to whenever a release is next tagged.

Nothing this session touched Stripe, published pricing, or the GitHub
App's installation-visibility setting.
