# @fixprove/web

FixProve landing page. Static export (`next build` with `output: "export"`)
for deployment to Cloudflare Pages at fixprove.dev.

**Status:** scaffold only (Session 0.2) — proves the package builds. The real
one-page site (thesis + email waitlist) is Session 0.3.

**License:** proprietary — see root `NOTICE.md`.
