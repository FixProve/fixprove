{
  "name": "@fixprove/billing",
  "version": "0.0.1",
  "private": true,
  "description": "FixProve billing -- Stripe subscription lifecycle, webhook signature verification, and the entitlement gate that turns the paid PR check on/off. TEST MODE ONLY (Session 4.12-J / B1).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/test/*.test.js",
    "lint": "echo 'lint: placeholder (matches app/ and worker/)'"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
