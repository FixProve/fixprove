{
  "name": "@fixprove/github-app",
  "version": "0.0.1",
  "private": true,
  "description": "FixProve GitHub App - posts a blocking Checks API status on PRs using the deterministic resolver engine (Milestone 2).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/test/*.test.js",
    "lint": "echo 'lint: placeholder (Session 2.x adds eslint)'"
  },
  "dependencies": {
    "@octokit/app": "^15.1.0",
    "@octokit/webhooks": "^13.3.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.16.11"
  }
}
